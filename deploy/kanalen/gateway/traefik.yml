entryPoints:
  websecure:
    address: ":443"

providers:
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true

certificatesResolvers:
  letsencrypt:
    acme:
      email: johansellstrom@activechain.dev
      storage: /letsencrypt/acme.json
      tlsChallenge: {}

api:
  dashboard: false

log:
  level: INFO
