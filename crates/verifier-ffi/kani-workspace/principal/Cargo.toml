# Verification-only metadata shim. The library target is the production principal source.
[package]
name = "activechain-principal"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false

[lib]
path = "../../../principal/src/lib.rs"

[dependencies]
activechain-protocol-types.workspace = true
