# Verification-only metadata shim. The library target is the production protocol-types source.
[package]
name = "activechain-protocol-types"
description = "Kani dependency entry point for the production ActiveChain protocol types"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false

[lib]
path = "../../../protocol-types/src/lib.rs"

[dependencies]
activechain-canonical-codec.workspace = true
sha3.workspace = true
