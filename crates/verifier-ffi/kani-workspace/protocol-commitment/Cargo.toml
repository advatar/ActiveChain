# Verification-only metadata shim. The library target is the production commitment source.
[package]
name = "activechain-protocol-commitment"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false

[lib]
path = "../../../protocol-commitment/src/lib.rs"

[dependencies]
activechain-canonical-codec.workspace = true
activechain-protocol-types.workspace = true
sha3.workspace = true
