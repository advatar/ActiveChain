# Verification-only metadata shim. The library target is the production bytecode verifier source.
[package]
name = "activechain-bytecode-verifier"
description = "Kani dependency entry point for the production ActiveChain bytecode verifier"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false
build = "../../../bytecode-verifier/build.rs"

[lib]
path = "../../../bytecode-verifier/src/lib.rs"

[dependencies]
activechain-canonical-codec.workspace = true
activechain-protocol-types.workspace = true
