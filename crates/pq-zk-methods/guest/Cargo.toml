[package]
name = "activechain-pq-zk-guest"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
risc0-zkvm = { version = "=3.0.5", default-features = false, features = ["std"] }
sha3 = { version = "0.10.8", default-features = false }
