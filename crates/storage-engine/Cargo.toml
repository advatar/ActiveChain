[package]
name = "activechain-storage-engine"
description = "Crash-safe sealed ledger segments and partition snapshots for ActiveChain"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false

[dependencies]
sha3.workspace = true

[dev-dependencies]
tempfile = "3.27.0"

[lints]
workspace = true
